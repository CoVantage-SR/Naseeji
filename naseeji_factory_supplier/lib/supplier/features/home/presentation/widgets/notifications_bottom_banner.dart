import 'package:flutter/material.dart';

class NotificationsBottomBannerWidget extends StatelessWidget {
  const NotificationsBottomBannerWidget({super.key});

  @override
  Widget build(BuildContext context) {
    // The notification popover is now attached as a speech bubble directly under the top bar bell icon in SupplierHomeHeader
    return const SizedBox.shrink();
  }
}


